# 课程池索引更新

更新 `课程/课程池总索引-V1.5.md`。

## 必改位置

1. KP 树行：把 `待建/占位` 改为 `🟡 素材已备`，挂上 markdown 母版相对链接。
2. 全量课程表行：状态、描述、文件链接与 KP 树一致。
3. 三处计数：主题进度表该行、合计行、footer 总规模。

不要标 `✅ 已上线`。上线需要立项、内审、质量评分人工门禁。

## 计数校验

```bash
awk -F'|' '/^\| [0-9]+\.0 /{g+=$3;a+=$4;b+=$5;c+=$6} END{print "课数="g" ✅="a" 素材="b" 待建="c}' 课程/课程池总索引-V1.5.md
```

把输出与合计行、footer 总规模比对。三处不一致时，不要报告完成。

## 来源留痕

来源不要放进课件正文。需要留痕时，在母版旁边生成 `<课程名>取材记录.md`，记录：

- IMA 是否启用，使用了哪些库。
- 仓库内参考了哪些教案、HTML、模板。
- 联网使用了哪些官方源和日期。
- 未启用 IMA 或来源不足时的人工复核风险。
